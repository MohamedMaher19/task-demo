import 'package:flutter/material.dart';

class FavortesPage extends StatelessWidget {
  const FavortesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('FavortesPage')),
      ),
    );
  }
}
